#if else

a=int(input("Enter your age: "))
if(a>=18):
    print("You can vote") 
    #after colon press enter to move to next line with space that space is indentation
    #indentation space bcs we are inside if block
else:
    print("You can't vote")


